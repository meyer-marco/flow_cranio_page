/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    //TODO: uncomment for build prep
    /*  unoptimized: true, */
  },
  //TODO: uncomment for build prep
  /*  output: "export", */
};

module.exports = nextConfig;
